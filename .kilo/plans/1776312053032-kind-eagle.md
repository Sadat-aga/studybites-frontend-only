# Plan: Transform Studybites Clone to Live Supabase-Backed App

## Overview
The codebase already has Supabase integration wired up. The task is to verify/migrate everything and ensure it actually works end-to-end.

---

## Task 1: Database Audit & Recovery

### 1.1 Inspect Current Schema
- Run `supabase status` to check project link
- Query Supabase directly to list all existing tables, RLS policies, storage buckets
- Compare against `studybites-spec.md` expected schema

### 1.2 Gap Analysis
Expected tables from spec:
- `users` ✓ (exists in migrations)
- `study_sets` ✓
- `folders` ✓ 
- `flashcards` ✓
- `flashcard_sessions` ✓
- `flashcard_attempts` ✓
- `flashcard_mastery_states` ✓ (added in 20260415123000)
- `mcq_questions` ✓
- `mcq_answers` ✓ (missing - spec has this, current uses choices JSONB)
- `mcq_sessions` ✓
- `mcq_attempts` ✓
- `summaries` ✓
- `summary_ratings` ✓ (added in 20260415123000)
- `subscriptions` ✓

**Key Gap**: `mcq_answers` table is missing - questions use JSONB for choices instead.

### 1.3 Migration
- Generate migration file in `supabase/migrations/` only if gaps found
- **DO NOT wipe existing data**

---

## Task 2: Type Safety

### 2.1 Verify Types
- `src/types/database.ts` already exists with full types from `supabase gen types`
- Verify types match current schema
- Run `npx supabase gen types typescript --project-id ywbqltvtgtukizumvnzv` if needed

### 2.2 Domain Types
- `src/types/auth.ts` already maps to database types via `Tables<"users">`, etc.
- Verify all types are used consistently

---

## Task 3: Auth Verification (Already Wired)

### 3.1 Current State
- `src/lib/supabase/client.ts`: Creates browser client with `@supabase/auth-helpers-nextjs` ✓
- `src/components/auth-provider.tsx`: Uses Supabase auth, queries `users` table, handles login/signup/logout ✓
- `src/components/protected-route.tsx`: Uses auth context `isAuthenticated` ✓

### 3.2 Verification Steps
1. Check `.env.local` has valid `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`
2. Test login flow: ensure `supabase.auth.signInWithPassword` works
3. Ensure RLS allows authenticated users to read their own user record

---

## Task 4: Library Data Wiring (Already Wired)

### 4.1 Current State
- `src/lib/study-data.ts`: 
  - `useLibraryDocuments()` queries `folders` with joins to `study_sets`, `mcq_questions`, `flashcards` ✓
  - `useFilePageData()` fetches single folder with joins ✓
  - `useExamQuestions()` queries `mcq_questions` with `mcq_answers` join ✓
  - `useFlashcardsDeck()` queries `flashcards` table ✓
  - `useSummaryResult()` queries `summaries` table ✓

### 4.2 Verification Steps
1. Ensure RLS allows authenticated users to query their own folders/study_sets
2. Verify joins work (RLS on joined tables)
3. Test with actual user login

---

## Implementation Order

1. **Inspect Supabase** - Run CLI to see current project state, check for data
2. **Verify/Update Types** - Regenerate types if needed
3. **Test Auth Flow** - Try login, fix any auth issues
4. **Test Library** - After login, verify library loads
5. **Test File Page** - Navigate to file page, verify no console errors
6. **Fix Any Issues** - Address RLS, missing columns, etc.

---

## Key Files to Test

- `/authenticate` - Login/signup forms
- `/library` - Main dashboard with documents
- `/library/files/[fileId]` - File overview page
- `/library/files/[fileId]/summary` - Summary page

---

## Success Criteria

1. User can sign up/login with real Supabase auth
2. Library page shows real documents from Supabase (not mock)
3. File page loads without console errors
4. Navigation between all pages works